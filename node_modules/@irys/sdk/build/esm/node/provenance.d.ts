import { Provenance } from "../common/provenance.js";
import { UploadReceipt } from "../common/types.js";
import { CreateAndUploadOptions } from "../common/types.js";
import NodeIrys from "./irys.js";
export declare class NodeProvenance extends Provenance {
    irys: NodeIrys;
    constructor(irys: NodeIrys);
    uploadFile(path: string, opts?: CreateAndUploadOptions): Promise<UploadReceipt>;
    uploadFolder(path: string, { batchSize, keepDeleted, indexFile, interactivePreflight, logFunction, manifestTags, itemOptions, }?: {
        batchSize: number;
        keepDeleted: boolean;
        indexFile?: string;
        interactivePreflight?: boolean;
        logFunction?: (log: string) => Promise<void>;
        manifestTags?: {
            name: string;
            value: string;
        }[];
        itemOptions?: CreateAndUploadOptions;
    }): Promise<UploadReceipt & {
        receipts: Map<string, UploadReceipt>;
    }>;
}
