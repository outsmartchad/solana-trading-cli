import BigNumber from "bignumber.js";
import type { BrowserProvider } from "ethersv6";
import type { Tx } from "../../../common/types.js";
import EthereumConfig from "../../tokens/ethereum.js";
export declare class EthereumEthersV6 extends EthereumConfig {
    protected provider: BrowserProvider;
    createTx(amount: BigNumber.Value, to: string, _fee?: string | undefined): Promise<{
        txId: string | undefined;
        tx: any;
    }>;
    getTx(txId: string): Promise<Tx>;
    ready(): Promise<void>;
}
